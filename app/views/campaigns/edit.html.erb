<div class="page-header">
  <h1>Editing Campaign
		<div class='pull-right'>
			<%= link_to "Back to Home", root_path, :class => 'btn' %>
		</div>
	</h1>	
</div>

<%= render 'form' %>

<%= render 'calendar', :campaign_dates => @campaign_dates %>
