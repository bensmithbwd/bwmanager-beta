<%= simple_form_for(@keyphrase, :html => {:class => 'form-horizontal' }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :keyphrase %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to "Cancel", keyphrases_path, :class => 'btn pull-right' %>
  </div>
<% end %>
